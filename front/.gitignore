# Logs. frontend dev server/build tool 로그는 실행마다 달라진다.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Installed dependencies. package-lock.json으로 재설치 가능하므로 repository에 넣지 않는다.
node_modules
dist
dist-ssr
*.local

# Editor directories and files. 개인 IDE/OS 설정은 공유하지 않는다.
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
